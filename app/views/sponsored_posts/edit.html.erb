<h1>Edit Sponsored Post</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for sponsored posts</p>
     <ul>
       <li>Joy and pain i slike sunshine and rain..</li>
       <li>Don't use the letter "F".</li>
       <li>Content creation for product,service.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for [@sponsored_post.topic, @post] do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter sponsored post title" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter sponsored post body" %>
       </div>
         <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
